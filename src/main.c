#include <stdio.h>
#include <stlib.h>
#include <math.h>
#include <ncurses.h>
#include <menu.h>

int main () {
    //Program checks for previously saved data based on user name passed from the terminal
    //Any saved data is loaded into the Program
    //ncurses window is initialized and the user sees a menu to view/edit/create projects.
}
